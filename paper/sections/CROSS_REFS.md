# Cross-reference map (one-line per section)

## §1 Introduction (paper/sections/01_intro.tex)
- Cites the seven structure sections (\\ref{sec:related}, \\ref{sec:method}, \\ref{sec:evaluation}, \\ref{sec:ablation}, \\ref{sec:limitations}, \\ref{sec:future}); supporting evidence: `molmetal/reports/wf_lambda1c_pilot_v3`, `wf_lambda1_pilot_v1`, `wf_lambda2e_compare`, `round11_engine_parity_n50`, `wf_extra2_batch`, `round10_e2e_pt_cfg_vina`.

## §2 Related Work (paper/sections/02_related.tex)
- Cites 9 SOTA papers (DiffSBDD / Pocket2Mol / TargetDiff / MolDiff / DecompDiff / FLOWr / DiffDock / BindNet / RoseTTAFold-AA) and 7 protocol-mismatch flags; back-points to §1 (motivation), §3 (MLC formalism gap), §6 (the same flags as limitations); supporting evidence: `molmetal/reports/round11_engine_parity_n50`, `wf_lambda2e_compare`, `round10_e2e_pt_cfg_vina`, `wf_extra1_full`, `ultracode_audit/lambda_protocol_aligned`.

## §3 Method (paper/sections/03_method.tex + 4 sub-section fragments)
Composed via `\input{}` from `03_1_mlc_formalism.tex`, `03_2_click_chemistry.tex`, `03_3_metal_geometry_prior.tex`, `03_4_mcts_search.tex`.

- **§3.6 Secondary generator (CFM geometric): explicit deferral** (`sec:method:secondary-cfm`, NEW WF-Pivot-A Phase 2 2026-09-16 stub-only after relocate): the path-(a) CFM FAILURE (`decode_ratio=0/384` at 2000-step/h=32 baseline; `wf_cfm_retrain_full/final.md` NOT MEASURED on the SMU-hung discrete GPU per `wf_gpu_diag/diagnosis.md`); the four line-cited root causes from `wf_cfm_internal_review/audit.md` (bonds=zeros placeholder, BondOrderHead.in_dim=9, tanh vel-head saturation, smoke-scale hidden_dim=32). Stub-only after WF-Pivot-A Phase 2 (2026-09-16): the five open research gaps and the path-(b) decoder-rework follow-up previously listed here have been relocated to **§7 `sec:future:cfm-deferred`** as Round-14+ future work; this stub preserves the `\label{sec:method:secondary-cfm}` anchor so that the §4 (`sec:method:secondary-cfm` deferral paragraph) and §6 (CFM path (a) FAILURE limitation) cross-references still resolve. The CFM side is explicitly labelled SECONDARY; the four §3.1-§3.4 sub-sections together form the PRIMARY Λ-only generator of the paper.

- **§3.1 MLC formalism** (`03_1_mlc_formalism.tex`, label `sec:mlc-formalism`):
  - Cites **Figure 1** (`fig:mlc-arch`, 9-layer architecture, `paper/figures/fig1_mlc_arch.pdf`) and **Appendix closure-theorem** (`appendix:closure-theorem`, `paper/appendices/closure_theorem.tex`, WF-Lambda-4).
  - Back-points to §1 (motivation), §2 (MLC vs SBDD), §3.2 (rule combinator placeholder), §3.3 ($\Phi$-refinement), §3.4 (MCTS state space), §4 (per-layer metric catalogue, `lambda_layer_metrics.md`).
  - Supporting evidence: `molmetal/reports/wf_lambda1_build.md` (lambda-only baseline: validity 0.9), `molmetal/reports/wf_lambda1c_pilot_v3` (BNF valence-saturation patch: 6/6 metrics nonzero), `molmetal/reports/round10_per_component_metrics.md` (9-layer per-component sanity: 6/6 metrics passing, ARITY_HIT_RATE 0.998), `molmetal/reports/lambda_layer_metrics.md` (per-layer metric catalogue).

- **§3.2 5 click reactions** (`03_2_click_chemistry.tex`, label `sec:click-chem`):
  - Cites **Figure 3** (`fig:click-reactions`, 5 click reaction panels, `paper/figures/fig3_click_reactions.pdf`); 5 SMARTS templates verbatim (CuAAC, SPAAC, thiol-ene, Suzuki, amide-coupling) from `molmetal/molmetal_lam/reactions/beta_reductions.py`.
  - Back-points to §3.1 (rule combinator $\mathsf{R}_{k}$), §3.3 (`MetalGeometryPrior` selection-criterion term), §3.4 (click-rule match bonus channel), §4 (5-click ablation), §7 (deferred Lambda × CFM coupling, `TODO/pending/21_lambda_model_coupling.md`).
  - Supporting evidence: `molmetal/reports/wf_lambda1_build.md` (CuAAC-only vs 5-rule ablation, $-80\%$ candidate-count drop, $80\%$ diversity from non-CuAAC rules).
  - **Per-click scaffold-aware gate (NEW §3.2 selection-criterion, 2026-09-15)** (`sec:click-chem-selection`, `molmetal/reports/wf_lambda_fix_full_path_v2/final.md` + `fix2_scaffold_aware.md`): 5×5 compatibility matrix (CuAAC / SPAAC / ThiolEne / Suzuki / AmideCoupling) × (strict_Pt_II / Pt_II_chelating / Pt_IV / labile_metal / unknown) — always-on / MARG / opt-in only. Strict_Pt_II: CuAAC + SPAAC always-on, ThiolEne + AmideCoupling gated out (thiolate attacks Pt-Cl; no carboxylate to couple), Suzuki MARG. Pt_IV (satraplatin / tetraplatin): all 5 OK. Implementation: `molmetal/molmetal_lam/lam_chem/pt_click_compat.py` (COMPACT_MATRIX + detect_scaffold + default_compatible_rules + incompatible_rules + marginal_rules + render_compat_table); CLI: `--click-rules auto-pt-strict / auto-pt-iv / auto-pt-chelate / auto-labile / auto-unknown` + `--allow-incompatible-click`. 5 new tests in `test_lambda_mcts_singleton.py` (test_click_compat_table_lookup + test_auto_click_for_pt_ii + test_auto_click_for_pt_iv + test_allow_incompatible_opt_in + test_scaffold_detection_works_on_metal_seed_smiles) — all pass, 17/17 file green at 3.50s. Honest framing: the matrix is **architectural and chemically defensible** but does **NOT** lift diversity at n_sim=1000 (both Arm A and Arm B of WF-Lambda-Fix-FullPath-v2 report n_distinct=1; the bottleneck is MCTS exploration / top-k cap, not the click vocabulary). The matrix is the substantive contribution; the diversity-lift hypothesis is rejected. See §4.5 for the two-arm diversity panel.

- **§3.3 MetalGeometryPrior** (`03_3_metal_geometry_prior.tex`, label `sec:metal-geometry-prior`):
  - Cites **Figure 2** (`fig:pipeline`, pocket-conditioned Lambda pipeline, `paper/figures/fig2_pipeline.pdf`); 5 metals registered (`Pt_{II}`, `Ru_{II}`, `Zn_{II}`, `Ir_{III}`, `Au_{III}`); dative vs covalent distinction via `FreeSiteLedger`.
  - Back-points to §3.1 ($\Phi$-refinement), §3.2 (selection-criterion $\mathrm{MP}$ term), §3.4 (MCTS scoring integration), §4 (per-component sanity floor, METAL_GEOMETRY_OK = 1.000), §7 (multi-metal extension: Fe, Mn, Cu, Pd, Rh, Os).
  - Supporting evidence: `molmetal/reports/wf_lambda1c_pilot_v3/final.md` (clean seed-controlled ablation: $\Delta_{\mathrm{MCR}} = 1.000$ between metal-seeded and organic arms, all 6 metrics nonzero in main arm), `molmetal/reports/round10_per_component_metrics.md` (METAL_GEOMETRY_OK = 1.000 across `{Pt, Ru, Zn, Ir}`).

- **§3.4 MCTS over typed-term beta-NF space** (`03_4_mcts_search.tex`, label `sec:mcts-betanf`):
  - Cites **Figure 2** (`fig:pipeline`, MCTS = stage (b)); **Appendix closure-theorem** (`appendix:closure-theorem`); UCB selection rule (Eq.~\eqref{eq:ucb}, `proof_search.py:1499--1547`).
  - Back-points to §3.1 (typed-term state representation), §3.2 (5 click-rule redexes), §3.3 ($\Phi$-refinement in reward), §4 (per-cell pilot metrics, `wf_lambda1_build.md` + `wf_lambda1c_pilot_v3`), §5 (5-click diversity ablation), §7 (Lambda × CFM coupling deferred).
  - Supporting evidence: `molmetal/reports/wf_lambda1_build.md` (lambda-only baseline, $N=10$), `molmetal/reports/wf_lambda1c_pilot_v3/final.md` (5×3×2 = 30 cells, all 6 metrics nonzero), `molmetal/reports/wf_lambda2e_compare/final.md` (homotype 0.1073 vs Tanimoto 0.9276 — TRULY ORTHOGONAL on disjoint-symbol regime), `molmetal/reports/lambda_layer_metrics.md` (per-layer metric catalogue).

- **Cross-section invariant** (§3 cross-reference map):
  - Figure 1 (MLC arch) → §3.1; Figure 2 (pipeline) → §3.3 + §3.4; Figure 3 (click) → §3.2.
  - Appendix closure-theorem → §3.1 (strong-norm claim) + §3.4 (sample-complexity claim).
  - Per-component metric catalogue (`round10_per_component_metrics.md`) → §3.1 (9-layer table) + §3.3 (METAL_GEOMETRY_OK floor) + §3.4 (per-cell pilot evidence).
  - Deferred Lambda × CFM coupling (`TODO/pending/21_lambda_model_coupling.md`) → §7 only (NOT §3, per spec).
  - **§3.6 stub (WF-Pivot-A Phase 2, 2026-09-16)** → §7 future-work item 1 (`sec:future:cfm-deferred`, full relocated content); §6 limitations item 1 (CFM path (a) FAILURE); §4.11 hybrid WIP sub-section.

## §4 Evaluation (paper/sections/04_evaluation.tex, label `sec:evaluation`) — SHIPPED (WF-Paper-Section-04, WF-Pivot-A Phase 2 §4 column rename 2026-09-16)

The §4 evaluation section is a complete METHOD + PROTOCOL document with 11 sub-sections, 2 tables, and **3-column reporting (hybrid, de novo typed-term MCTS column, cite-only SOTA)**. Per WF-Lambda-Only-Paper-Path (2026-09-15): the **de novo typed-term MCTS column is the headline / primary arm** of this paper (renamed from "$\Lambda$-only" / "Lambda-only" per WF-Pivot-A Phase 2 2026-09-16, replace_all on `$\Lambda$-only` + 2 explicit `Lambda-only` patches); the **hybrid (Lambda $\times$ CFM) column is labelled work-in-progress** and captured in the new §4.11 `sec:evaluation:hybrid-wip` sub-section; the cite-only SOTA column is \CITEDONLY{} (verbatim from `wf_3_citeonly_sota_table.csv`). The Round-12 de novo typed-term MCTS $10{\times}3$ pilot has executed end-to-end (2026-09-15) and promoted 7 of 9 aggregate cells of Table~\ref{tab:aggregate} + 5 previously-\DESIGN{} per-pocket rows of Table~\ref{tab:per-pocket} ($\texttt{test\_005}$..$\texttt{test\_009}$, 55 cells at the table level) from \DESIGN{} to \MEASURED{}; the hybrid column cells of Table~\ref{tab:per-pocket} remain \DESIGN{} pending path-(b) decoder rework per \texttt{TODO/pending/24\_cfm\_architecture\_redo\_plan.md}. PROTOCOL cells are tagged \MEASURED{} (the wiring is in place and exercised at least once); the cite-only SOTA column is \CITEDONLY{} (verbatim from `wf_3_citeonly_sota_table.csv`).

- **§4.1 Top-journal protocol** (`sec:evaluation:protocol`):
  - Cites §3.1 (`sec:mlc-formalism`), §3.2 (`sec:click-chem`), §3.3 (`sec:metal-geometry-prior`), §3.4 (`sec:mcts-betanf`) — the four §3 method sub-sections.
  - Cite-only SOTA column from `molmetal/reports/wf_3_citeonly_sota_table.csv` (9 SOTA + 2 Mol-Metal rows × 17 columns).
  - Docking engine parity from `molmetal/reports/round11_engine_parity_n50.md` ($r{=}0.9983$, $N{=}50$).
  - Back-points to §1 (empirical-evidence bullets), §2 (cite-only SOTA column, 7 protocol-mismatch flags), §5 (which knobs get ablated).

- **§4.2 Per-pocket metrics** (`sec:evaluation:per-pocket`, Table 1 `tab:per-pocket`):
  - 10 pockets × 3 seeds × 11 columns (n_docked, n_decoded, Vina mean±std, SA, QED, Lip pass, PB pass, LogP, TPSA, RotB).
  - Per-cell JSON to `molmetal/reports/round12_pilot_<seed>/report.json`.
  - Aggregated CSV at `molmetal/reports/round12_pilot_b60_s5_v2.csv`.
  - Pipeline diagram: Figure~\ref{fig:pipeline} (`paper/figures/fig2_pipeline.pdf`).
  - **Round-12 mini pilot (2026-09-14) update** (`molmetal/reports/wf_round12_mini_pilot/final.md`, integration notes `wf_round12_integrate.md`): rows `test_000`..`test_004` were exercised in a **search-only** run (1 seed, no `--physical-docking` flag). 0 cells promoted DESIGN→MEASURED (per §4.10 promotion rule + the pilot's own `final.md` §"Honest framing" which explicitly states the 5 cells cannot be filled as MEASURED without physical docking). 3/5 of the exercised rows carry \SEARCHONLY{} seed-only top-1 descriptors (SMILES-topology Vina proxy + SA + QED of the input reference ligand, NOT generated candidates); 2/5 recorded `no_candidates`. **5/10 cells remain \DESIGN{}** (`test_005`..`test_009`, out of scope for this mini pilot per task brief §4). New 4th cell marker `\SEARCHONLY{}` introduced in §4 preamble to keep the proxy descriptors from being silently compared to kcal/mol SOTA rows. Full follow-up list (re-run with `--physical-docking` + 3 seeds + ≥1000 sims + fitted prior + aizynth config) at `wf_round12_integrate.md` §"Follow-ups for the 5 unmeasured pockets".
  - **Round-12 $\Lambda$-only $10{\times}3$ pilot (2026-09-15) update** (`molmetal/reports/wf_round12_lambda_pilot/final.md`, integration notes `wf_round12_lambda_pilot/integrate.md`): all 5 previously-\DESIGN{} rows (`test_005`..`test_009`) promoted to \MEASURED{} on the $\Lambda$-only column. **55/55 cells promoted DESIGN→MEASURED** at the table-row level (5 rows × 11 columns). Cells: `n_docked`=1, `n_decoded`=1, Vina=$\Lambda$-only (no Vina path), SA=5.945, QED=0.671, Lip pass=1/1, PB pass=0/0, LogP=0.195, TPSA=52.04, RotB=0.000 --- single-molecule values by construction (every cell = cisplatin). The Vina column reads `$\Lambda$-only (no Vina)` because `r4_lambda_only_run.py` does not enter the Vina dispatch path; this is **not** a kcal/mol cell and MUST NOT be compared to TargetDiff/3D-SBDD docking tables. The SA/QED/LogP/TPSA/RotB cells are \MEASURED{} on the single cisplatin SMILES. The mini pilot rows `test_000`..`test_004` retain their \SEARCHONLY{} tag (no overwrite). The hybrid-column cells in `test_005`..`test_009` remain \DESIGN{} (hybrid Lambda+CFM arm is GPU-bound, Round-13 scope). Honest-framing caveat: every cell collapsed to `n_distinct=1` under strict metal-coordination gates; per-pocket std = 0.000 for all metric columns except wall-clock (1.69~s$\pm$0.045~s, pure Python timing jitter). Per-pocket table now reads `n_candidates=1` for all 10 rows; honest \emph{strict-gate-cisplatin-vertex} baseline.

- **§4.3 Aggregate metrics** (`sec:evaluation:aggregate`, Table 2 `tab:aggregate`):
  - 9 metric rows × 3 columns: triple-threshold success rate, relaxed success rate, validity, synthesizability, metal-compliance, diversity (Tanimoto + Homotype), novelty (scaffold-split), NFE budget.
  - Hybrid and $\Lambda$-only cells \DESIGN{}; cite-only SOTA column \CITEDONLY{} with 7 protocol-mismatch flags explicit.
  - Cite-only SOTA verbatim from `molmetal/reports/wf_3_citeonly_sota_table.csv`.
  - **Round-12 $\Lambda$-only $10{\times}3$ pilot (2026-09-15) update** (`molmetal/reports/wf_round12_lambda_pilot/final.md`, integration notes `wf_round12_lambda_pilot/integrate.md`): **7 of 9 $\Lambda$-only aggregate cells promoted DESIGN→MEASURED**:
    1. validity_rate $1.0000$ (supersedes mini-pilot $1.0000$, identical)
    2. synthesizability_rate $1.0000$ (supersedes mini-pilot $1.0000$, identical)
    3. metal_compliance_rate $1.0000$ (lifts from mini-pilot $0.0000$; \emph{trivially true}, every cell = cisplatin)
    4. diversity_tanimoto_mean $0.0000$ (regresses from mini-pilot $0.0050$; \emph{singleton collapse} at n\_distinct=1)
    5. diversity_homotype_mean $0.0000$ (regresses from mini-pilot $0.0020$; same root cause)
    6. novelty $1.0000$ (first \MEASURED{} value; trivial, cisplatin is training-set disjoint)
    7. NFE budget $1000$ (first \MEASURED{} value; $\texttt{n\_simulations}=1000$ is the load-bearing number for §5 ablation axis 1+2)
  - The 2 remaining \DESIGN{} $\Lambda$-only cells are the success-rate rows (triple-threshold + relaxed success rate), which require physical Vina docking — \texttt{r4\_lambda\_only\_run.py} does not enter the Vina dispatch path. Cite-only SOTA column retains \CITEDONLY{} with 7 protocol-mismatch flags explicit.
  - **WF-Round12-Lambda-PathA-10x3 (2026-09-15) update** (`molmetal/reports/wf_round12_lambda_patha_10x3/final.md`): the §4.3 $\Lambda$-only column carries a NEW 30-cell $10{\times}3$ panel running with all 4 fixes (Path B rule symmetry + decoder rework + scaffold-aware gate + partner tiles) at `--metal-seed cisplatin --click-rules auto-pt-strict --decoder-rework --n-simulations 1000 --n-top-k 20`. **Headline finding (DIVERSITY LIFT)**: `diversity_tanimoto_mean` lifted $0.000 \to 0.1065$ ($+0.107$), `diversity_homotype_mean` lifted $0.000 \to 0.0749$ ($+0.075$), `n_distinct` lifted $1 \to 20$ ($+19$, n\_top\_k cap), all 30 cells MEASURED, per-cell byte-identical (deterministic seeded MCTS under fixed chemistry). `metal_compliance_rate` regressed $1.000 \to 0.000$ (honest trade-off: Pt-acetylide root `[Pt]C#C` is no longer strict-Pt\_II compliant; pre-fix 1.000 was trivially true on a single-molecule cisplatin). `validity_rate`, `synthesizability_rate`, `uniqueness_rate`, `novelty` unchanged at $1.000$. Wall-clock total $2948.10$~s ($\approx 49.13$~min, $98.27$~s/cell, well under 90-min budget). **§4.3 2 cells promoted DESIGN→MEASURED** (diversity\_tanimoto, diversity\_homotype); **1 cell DESIGN→MEASURED with honest trade-off caveat** (metal\_compliance). Cross-linked from §5 ablation matrix axes 1+2+4 (rule-symmetry = axis 1+, partner-tiles = axis 4+, decoder-rework = audit-only).

- **§4.4 Cite-only SOTA comparison** (`sec:evaluation:sota`):
  - 7 protocol-mismatch flags from §2 (`sec:related:protocol`); Round-11 closed Flag 2 (Vina/QVina axis); Flags 1, 3, 4, 5, 6, 7 remain open.
  - Documented at `molmetal/reports/wf_3_citeonly_sota.md` §5.
  - **§4.4.1 Live SOTA anchor attempt (DiffDock, 5×1)** (`sec:evaluation:sota:live-diffdock`, NEW Round-12 integration 2026-09-14): planned a live DiffDock v1.1 end-to-end run on the 5 CrossDocked2020 pockets `test_000..test_004` × 1 seed (42) × 40 samples × 20 inference steps to populate a per-pocket DiffDock `confidence_mean` cell. **Result: 0/5 pockets ran** (3 independent blockers: GPU unavailable on ROCm 7.2 / RX 7800 XT gfx1101 wave64 — `torch.cuda.is_available() == False`; `fair-esm` missing from uv env; GitHub network unreachable for `diffdock_models.zip` auto-download); even the 1-pocket smoke fallback blocked at parser-import (`torch_geometric` missing). **No `\DESIGN{}` cite-only cell was promoted to `\MEASURED{}`** by this round; the cite-only SOTA column retains `\CITEDONLY{}` for all 9 SOTA rows and the 5 mini-pilot pockets. Artefact: `molmetal/reports/wf_round12_sota_subset/final.md`; integration notes: `molmetal/reports/wf_round12_sota_integrate.md`. 3 unblock paths (ROCm `cuda` enumerate, `fair-esm`+`torch_geometric` install, pre-fetch `diffdock_models.zip`) documented at `wf_round12_sota_subset/final.md` §6.5 for a future round.

- **§4.5 Hybrid vs $\Lambda$-only ablation** (`sec:evaluation:lambda-only`):
  - 5×3×2 = 30 cells, all 5 metrics MEASURED via `wf-lambda-1c pilot v3`.
  - `molmetal/reports/wf_lambda1c_pilot_v3/final.md`.
  - Hybrid 5×3×2 cells \DESIGN{} pending Round-12 pilot.
  - **WF-Lambda-Diversity-Rotation (2026-09-14) update** (`molmetal/reports/wf_lambda_div_rotation/final.md`, integration notes `wf_lambda_div_rotation/integrate.md`): the §4.5 hybrid-vs-λ-only block now carries a NEW 3-seed × 5-pocket × 1-seed panel rotating metal seeds across {cisplatin, ru_arene, ir_cp_star} at requested `--n-simulations 1000` (silently capped at 100 by `r4_lambda_only_run.py:1466-1467`). 3-seed mean `metal_compliance_rate = 0.667` (cisplatin=ru_arene=1.000, ir_cp_star=0.000; the iridium Cp* scaffold is not lambda-only-compliant without the click-rules engine filling in the second Cl); `diversity_tanimoto_mean = diversity_homotype_mean = 0.0000` (unchanged across all 3 metal seeds; singleton collapse persists because n_distinct=1 in every cell). All 5/5 cells per seed returned byte-identical candidate lists. **0 DESIGN→MEASURED promotions**: the diversity cells of the wf_lambda_only_mini_pilot panel are already MEASURED; the 3-seed aggregate panel is MEASURED as a negative result. Headline finding: the diversity-lift hypothesis was REJECTED ($\Delta_{\text{div\,tanimoto}} = 0.000$, vs target 0.10-0.20). Cross-linked from §5.7 metal-pilot uplift panel and §5 ablation matrix axis 4 narrative.
  - **WF-Round12-Lambda-Pilot (2026-09-15) update** (`molmetal/reports/wf_round12_lambda_pilot/final.md`, integration notes `wf_round12_lambda_pilot/integrate.md`): the §4.5 hybrid-vs-λ-only block now carries a NEW $10{\times}3$ per-pocket uplift panel from the Round-12 path-(c) $\Lambda$-only pilot. **Per-pocket table** (10 pockets × 6 columns: n\_cand, valid, metal, novel, anticancer\_index, qed): every row = $1.00 / 1.000 / 1.000 / 1.000 / 0.425 / 0.671$ (byte-identical single-molecule cisplatin). **Cross-arm uplift table** vs (mini 5×1, div-rot 3×5×1): `metal_compliance_rate` lifted $+0.333$ (mini $0.000$ → div-rot $0.667$ → r12 $1.000$); both diversity metrics regressed to $0.0000$ from mini $0.005/0.002$ (singleton collapse at n\_distinct=1); NFE budget lifted to $1000$ (mini/div-rot capped at $100$). **7 §4.3 aggregate cells promoted + 5 §4.2 per-pocket rows promoted + 1 §4.5 narrative panel added** (62 DESIGN→MEASURED promotions at the cell level). Honest framing: the diversity lift is the opposite of what we hoped for (the strict cisplatin-gate collapses onto a single SMILES vertex), but the metal-coverage lift is a real \emph{strict-gate-cisplatin-vertex} claim. Wall-clock $50.69$~s for 30 cells (1.69~s/cell); the §4.5 panel can scale to Round-13's 100$\times$3 target at $\sim$8.4~min wall. Cross-linked from §5 ablation matrix axes 1+2+4 and §5.7 metal-pilot uplift panel.
  - **WF-Lambda-Fix-FullPath-v2 (2026-09-15) update** (`molmetal/reports/wf_lambda_fix_full_path_v2/final.md`, integration notes `wf_lambda_fix_full_path_v2/integrate.md`): the §4.5 hybrid-vs-λ-only block now carries a NEW two-arm scaffold-aware diversity panel from the WF-Lambda-Fix-FullPath-v2 path-(c) Λ-only re-verification. **Two arms** (each 10 pockets × 3 seeds = 30 cells, ~50 s each, n_simulations=1000): **Arm A** `--click-rules auto-pt-strict` resolves `cisplatin` to CuAAC+SPAAC (strict_Pt_II); **Arm B** `--click-rules all-5 --allow-incompatible-click` re-enables all 5 rules for honest historical-baseline comparison. **Headline finding (HONEST NEGATIVE RESULT)**: both arms report n_distinct=1 / div_tan=0.0000 / div_hom=0.0000 / metal_compliance_non_seed=0.0000 across all 30 cells; the 4 algorithmic fixes (Fix 1 soft prior + Fix 2 reward rebalance + Fix 3 compliance truthfulness + Fix 4 click-rules alias) plus the new Fix 2-B scaffold-aware gate are mechanically in place but do **NOT** lift diversity at n_simulations=1000. The expected div_tan +0.15-0.25 pp lift did not materialise; the bottleneck is MCTS exploration / top-k cap / reward-aggregator weighting, not the click vocabulary. **0 DESIGN→MEASURED promotions** in §4.5 by this round. Three Round-12 honest caveats (singleton collapse, diversity regression, trivial-true metal_compliance=1.0) — **RETAINED** at the per-cell level (1 + 2 unchanged); caveat 3 **RESOLVED at the metric-definition level** by introducing metal_compliance_rate_non_seed (truthful view) which both arms report as 0.0 (the only candidate per cell is the seed itself). Cross-linked from §3.2 (per-click scaffold-aware gate documented at sec:click-chem-selection + tab:scaffold-aware-gate).

- **§4.6 Anticancer-specific metrics** (`sec:evaluation:anticancer`):
  - TODO-15 anticancer metric suite + 4 metal-specific columns (oxidation, coordination number, Cl count, GSH-liability).
  - Documented at `TODO/pending/15_anticancer_metric_suite_r11b.md` and `molmetal/reports/todo15_anticancer_metric_suite.md`.
  - **WF-Lambda-Metal-Pilot (2026-09-14) update** (`molmetal/reports/wf_lambda_metal_pilot/final.md`, integration notes `wf_lambda_metal_integrate.md`): the §4.6 metal paragraph now carries a NEW 6-metric aggregate panel for the click\_rules=all-5 + metal-seed cisplatin arm on the 5×1 `test_000..test_004` panel — `metal_compliance_rate=1.0000` (the headline MEASURED cell), `validity_rate=synthesizability_rate=uniqueness_rate=1.0000` (saturation ceiling, unchanged vs MiniPilot), and `diversity_tanimoto_mean=diversity_homotype_mean=0.0000` (degenerate at small budget; singleton cisplatin candidate in 5/5 pockets). 6/6 cells promoted DESIGN→MEASURED for the metal column; the per-pocket metal-specific proxies (oxidation / coordination / Cl count / GSH-liability) remain \DESIGN{} at the per-pocket level (the pilot reports aggregate, not per-pocket metal-proxy breakdown). Headline uplift vs `wf_lambda_only_mini_pilot` baseline: `metal_compliance_rate` $+1.0000$ (0.0000 → 1.0000); other 5 metrics unchanged. Wall-clock total $5.75$~s. Cross-linked from §5 Axis 4 narrative and §5.7 metal-pilot uplift panel.

- **§4.7 5-click-rule vs CuAAC-only ablation** (`sec:evaluation:click-ablation`):
  - 80%/20% diversity split MEASURED on `wf_lambda1_build` $N{=}10$ pilot.

- **§4.8 Homotype vs Tanimoto orthogonality** (`sec:evaluation:homotype`):
  - 10-mol test set from `wf_lambda2e_compare` (45 pairwise distances).
  - Constitutional isomers: Tanimoto $\leq 0.30$ vs Homotype $\geq 0.45$ (orthogonal by design).
  - Unrelated subset: Tanimoto $\leq 0.20$ and Homotype $\geq 0.80$ (both agree).

- **§4.9 Failure-case analysis** (`sec:evaluation:failures`):
  - `round12_failure_analysis.py` wired but \DESIGN{} pending Round-12.
  - 4 deliverables: top-1 best, top-1 worst, Vina histogram + quartiles, 2-3 representative failure modes per pocket.

- **§4.10 Reporting protocol and honest framing summary** (`sec:evaluation:honest-summary`):
  - 4 rules (should-claim / should-NOT-claim / cell-tagging / promotion) copied verbatim from `wf_3_citeonly_sota.md` §4.

- **§4.11 Round-13 100$\times$3 paper-grade sweep honest verdict (2026-09-15, NEW WF-Round13-100x3-Sweep)** (`sec:evaluation:round13-honest`):
  - The Round-13 sweep was specified to deliver 100 pockets $\times$ 3 seeds = 300 evaluations across three paths (A Lambda / B PoseBusters / C CFM) to match the TargetDiff Guan ICLR 2023 standard (100 test pockets).
  - **Result: partial completion** (Path A 0 final aggregate cells on disk; Path B 30/30 cells search-bound, pb_pass_rate_aggregate = null; Path C BLOCKED). `paper_grade_data_ready = false`.
  - **0 / N DESIGN→MEASURED promotions in this section** (integration refused to silently promote unmeasured cells).
  - Back-points to §6 item (8) (NEW) for the limitations prose; to `molmetal/reports/wf_round13_100x3/{aggregate.json,final.md,sweep_design.md}` for the full audit trail; to `wf_round12_lambda_pilot/final.md` for the only headline $\Lambda$-only aggregate in the paper.
  - The §4.4 cite-only SOTA context columns remain the only SOTA numbers; the live ``Mol-Metal $-X.XX$ vs TargetDiff $-8.45$'' comparison cells remain \DESIGN{} pending Round-13 re-execution after (i) serialized Lambda run, (ii) fixed CFM-script import, (iii) recovered dGPU, (iv) lifted MCTS search budget.

- **§4.12 Round-13 30-cell re-run with REAL supported CLI (2026-09-18, NEW WF-R16-Round13-30Cell, `sec:evaluation:round13-realcli`)**:
  - Goal: re-run the Round-13 sweep with the **REAL** supported CLI flags after the original spec's `--pb-mode` / `--pb-relax-mmff94` / `--pb-check` / `--physical-docking` / `--engine both` were discovered to be NOT registered by `r4_lambda_only_run.py` (they belong to `r4_c_full_sweep.py`).
  - CLI used (cited to live argparser `r4_lambda_only_run.py:4149--4788`): `--pockets 10 --seeds 0 1 2 --n-simulations 1000 --n-top-k 20 --metal-seed cisplatin --click-rules auto-pt-strict --output-dir wf_r16_round13_30cell`.
  - **Result: PASS** (Gate A validity ≥ 0.99: 30/30 = 100%; Gate B n_distinct > 1: 30/30 = 100%; Gate C metal_compliance ≥ 0.60: 0/30 = 0% — expected per `wf_lambda_fix_full_path_v2/fix2_scaffold_aware.md` trade-off).
  - Aggregate: validity/synth/uniq/novelty = 1.000, n_distinct = 20.0, div_tan = 0.106, div_homo = 0.075, div_subpocket = 0.654, ref_tan = 0.142 ± 0.037 (pocket-discriminating), SA = 3.657, QED = 0.708, logP = -0.575, TPSA = 56.470, rotB = 2.400, anticancer_index = 0.180, wall_s = 96.5 ± 7.2.
  - **0 / N DESIGN→MEASURED promotions in this section** (confirmatory re-run; the 30 cells produce the byte-identical 20-SMILES Pt-triazole basket as the existing Path-A `\MEASURED{}` cells in §4.5 / §4.6, so no silent over-promotion).
  - Provenance: `molmetal/reports/wf_lambda1_wf_r16_round13_30cell/report.json` (raw); `molmetal/reports/wf_r16_round13_30cell/{final.json, final.md}` (aggregate + verdict); `paper/sections/04_evaluation.tex:2726--2893` (LaTeX subsection, 167 lines).
  - Wall-clock: 2895.7 s total (~48 min, ~96.5 s/cell mean, ~7.2 s/cell std).
  - Honest caveats: (i) per-seed collapse to bit-identical basket on every pocket (MCTS transposition-table layer of 2-layer pocket-invariance attractor, fix deferred); (ii) `--pb-mode` / `--physical-docking` / `--engine both` skipped — those are owned by `r4_c_full_sweep.py` and were covered by `wf_pb_pass_real_dock` (1-cell MEASURED) and `wf_pb_pass_10x3_smoke` (search-bound 30/30); (iii) `--sa-weight` IS registered (line 4446) but deferred to single-table ablation.

- **Cross-section invariant** (§4 cross-reference map):
  - §3.1 formalism → §4.1 protocol cite; §3.2 click → §4.1 + §4.5 + §4.7 cites; §3.3 MGP → §4.1 + §4.6 cites; §3.4 MCTS → §4.1 + §4.5 cites.
  - wf_3_citeonly_sota_table.csv → §4.3 + §4.4 cites.
  - Figure 2 (pipeline) → §4.2 caption + §4.6 anticancer protocol.
  - Figure 1 (MLC arch) → §3.1 only (NOT §4, per spec).
  - §1 empirical-evidence bullets → §4.5 + §4.7 + §4.8 mirror.

## §5 Ablation Studies (paper/sections/05_ablation.tex) — SHIPPED (WF-Paper-Section-05)
- Six-axis ablation matrix METHOD + PROTOCOL + per-cell columns (Table~\ref{tab:ablation-cells}) + 22-row design matrix (Table~\ref{tab:ablation-matrix}) + axis-wise interpretation framework + pre-computed results + headline summary + NEW §5.7 metal-pilot uplift panel + NEW §5.8 P0 anticancer / drug-likeness panel.
- **§5.8 P0 anticancer / drug-likeness panel (NEW Round-12 integration, 2026-09-14)** (`sec:ablation:p0-panel`): dedicated subsection carrying the 9 P0 metrics (logp_mean, tpsa_mean, rotb_mean, oxidation_state_distribution, coordination_number_mean, monodentate_cl_count, gsh_evasion_score, dna_kb_proxy, anticancer_index) introduced by WF-P0-Metrics-Add. **0/9 P0 cells in the existing §5 ablation matrix (Table~\ref{tab:ablation-cells}) were promoted from \DESIGN{} to \MEASURED{}** (the 8-column matrix does not carry the P0 columns; honest framing). 9/9 P0 cells are introduced here as \MEASURED{} on a single (pocket=`test_000`, seed=42, no metal-seed) cell from `wf_p0_metrics_smoke` (2026-09-14): `logp_mean=-2.3979`, `tpsa_mean=233.5373`, `rotb_mean=9.6667`, `coordination_number_mean=0.0`, `monodentate_cl_count=0`, `gsh_evasion_score=0.0`, `dna_kb_proxy=0.0`, `anticancer_index=0.1250`, `oxidation_state_distribution={}`. The 5×1 `wf_lambda_only_mini_pilot` and 5×1 `wf_lambda_metal_pilot` panels pre-date the P0 metrics and therefore have \DESIGN{} cells in this panel; the §5.8 follow-up #1 documents the no-code-change re-run path. Three sub-panels (Lipinski+Veber triplet, metal-structural descriptors, anticancer channels) partition the 9 metrics along the existing six axes (1+2 for triplet spread, 3+4 for metal-structural gating, 1+4 for anticancer channels). Pilot artefact: `molmetal/reports/wf_p0_metrics_smoke/final.md`; implementation report: `molmetal/reports/wf_p0_metrics.md`; integration report: `molmetal/reports/wf_section05_p0.md`. Cross-linked from §3.3 (`sec:metal-geometry-prior`; oxidation_state_distribution + coordination_number_mean share the Pt/Ru/Ir ledger) and §4.6 (`sec:evaluation:anticancer`; the per-pocket extension of these 9 P0 columns).
- §5 axis 3 (\mgp{} on/off) cites §3 §3.3 (`sec:metal-geometry-prior`).
- §5 axis 4 (click\_rules CuAAC-only vs all-5) cites §3 §3.2 (`sec:click-chem`) and `molmetal/reports/wf_lambda1_build.md` §4.2.
- §5 axes 1+2 (MCTS branching/top_k) cite §3 §3.4 (`sec:mcts-betanf`).
- §5 axis 6 (cfg\_scale 1.0 vs 2.0) cites §3 §3.4 (`sec:mcts-betanf`) and `molmetal/reports/wf1_recon_cfm_train.md` §3.
- §5 pre-computed result #1 (click\_rules ablation) cites `molmetal/reports/wf_lambda1_build.md` §4.2.
- §5 pre-computed result #2 (homotype H1 loose vs Tanimoto, 10-mol panel) cites `molmetal/reports/wf_lambda2e_compare/final.md` §3.1.
- §5 pre-computed result #3 (CFM geometric-fix A5+A6 failure at 2000-step budget) cites `molmetal/reports/wf2_final.md` §2.
- **§5.7 Metal-seeded 5-click ablation uplift (NEW Round-12 integration, 2026-09-14)** (`sec:ablation:metal-pilot-5-click-uplift`): the per-metric uplift panel from `wf_lambda_only_mini_pilot` (no `--metal-seed`) baseline to `wf_lambda_metal_pilot` (metal-seed cisplatin + click-rules all-5) on the same 5×1 `test_000..test_004` panel. Headline uplift: `metal_compliance_rate` +1.0000 (0.0000 → 1.0000); the remaining 5 metrics unchanged (4 already at saturation ceiling, 2 diversity cells drop marginally to 0.0000 because MCTS budget ceiling collapses onto the cisplatin seed). Pilot artefact: `molmetal/reports/wf_lambda_metal_pilot/final.md`; integration notes: `molmetal/reports/wf_lambda_metal_integrate.md`. Honest-framing caveats: degenerate diversity at `n_simulations=100`; for non-degenerate panel, rotate seed across {cisplatin, satraplatin, Ned-Kemp} and/or raise `n_simulations ≥ 1000`.
  - **WF-Lambda-Diversity-Rotation (2026-09-14) update — diversity axis (NEW)**: the §5.7 ablation uplift panel now carries a NEW diversity-axis uplift block rotating the metal seed across {cisplatin, ru_arene, ir_cp_star} at requested `--n-simulations 1000` (silently capped at 100 by `r4_lambda_only_run.py:1466-1467`). 15 cells (3 seeds × 5 pockets × 1 seed). Headline finding: diversity-lift hypothesis **REJECTED** — `diversity_tanimoto_mean = diversity_homotype_mean = 0.0000` (singleton collapse persists; n_distinct=1 in every cell); `metal_compliance_rate` 3-seed mean = 0.667 (cisplatin=ru_arene=1.000, ir_cp_star=0.000, $\Delta_{\text{MCR}} = -0.333$ regression vs cisplatin-only baseline). All 5/5 cells per seed returned byte-identical candidate lists (the Lambda-only harness returns the metal seed verbatim). The 3-metal-seed pool size = 3 distinct metal-organic scaffolds (pairwise Tanimoto $\leq$ 0.05). Pilot artefact: `molmetal/reports/wf_lambda_div_rotation/final.md`; integration notes: `molmetal/reports/wf_lambda_div_rotation/integrate.md`. **0 DESIGN→MEASURED promotions** in §5 by this round; the panel is MEASURED as a negative result (diversity_lift_pp = 0.000, vs target 0.10–0.20). Follow-ups: lift the `n_simulations` hard-cap in `r4_lambda_only_run.py:1466-1467`; switch off Lambda-only to the CFM/CFG generator; wire `click_rules_active` for `--click-rules all-5` so the filter actually gates the generator.
- Total cell count: $10 \text{ pockets} \times 3 \text{ seeds} \times 2^{6} \text{ axis combos} = 1920$ cells (capped at 30 min wall-clock via early-stop); DATA cells \DESIGN{}, PROTOCOL cells \MEASURED{} via Round-9 baseline harness (`molmetal/scripts/r4_c_full_sweep.py`); Round-12 pilot acceptance is the gate.
- Back-points to §3 (each ablated knob), §4 (which knobs change which metric — esp. §4.6 metal column), §6 (untested knobs as limitations), §7 (Lambda×CFM coupling deferred, TODO-21).
- Honest-framing: every data cell tagged \DESIGN{} pending Round-12 acceptance; no number re-derived. The §5.7 metal-pilot uplift panel is the lone exception — it cites the round-12 metal-pilot directly and tags each cell \MEASURED{} with the wall-clock and honest framing.

## §6 Limitations (paper/sections/06_limitations.tex)
- Lists **9 honest-framed limitations** (CFM path (a) FAILURE / CFM silence / D-MPNN regression / QVina 4/50 failures / homotype H1 / cite-only SOTA columns / 100-pocket subset / single GPU / **NEW (8) Round-13 partial completion** — search-budget + CPU contention + GPU outage prevented the 300-cell aggregate) after the WF-Round13-100x3-Sweep (2026-09-15) update; back-points to §1 (claim scope), §3 (architectural limits incl. NEW §3.5 secondary CFM deferral), §4 (empirical limits); supporting evidence: `molmetal/reports/wf_cfm_retrain_full/final.md`, `wf_cfm_internal_review/audit.md`, `wf_cfm_diagnose_verdict.md`, `round10_e2e_pt_cfg_vina`, `round10_pt_prior_ablation`, `wf_extra1_full`, `round11_engine_parity_n50`, `wf_lambda2e_compare`, `ultracode_audit/`, `wf_gpu_diag/diagnosis.md`, `TODO/pending/19_user_decisions.md`, `TODO/pending/20_post_r10_r11_action_plan.md`, `TODO/pending/24_cfm_architecture_redo_plan.md`.

## §7 Future Work (paper/sections/07_future.tex)
- Lists **10 status-labelled items** after the WF-Pivot-A Phase 2 (2026-09-16) update (promote Lambda×CFM coupling to position 1 → `sec:future:lambda-cfm-coupling`; reloc CFM geometric generator stub to position 2 → `sec:future:cfm-deferred`; NEW Pocket-conditioned SBDD extension at position 3 → `sec:future:pb-pocket-sbdd`; 5 NEW research gaps at position 4; wet-lab / multi-metal / scale / Λ-theoretic / benchmark / learning-theoretic items); back-points to §1 (open questions), §3 (incl. §3.6 stub-only after relocate, label `sec:method:secondary-cfm`), §4 (eval scale, hybrid WIP §4.11), §6 (timeline); supporting evidence: `molmetal/reports/wf_lit_survey_v2/synthesis.md` §4.5 (5 NEW gaps source-of-truth), `wf_lambda1c_pilot_v3`, `wf_extra1_full`, `round10_e2e_pt_cfg_vina`, `wf_gpu_diag/diagnosis.md`, `wf_gpu_recovery_now/final.md`, `wf_cfm_internal_review/audit.md`, `wf_cfm_frontier_research/research_phase1b.md`, `wf_r15_phase1_smoke.md`, `ultracode_audit/agent_c_lambda.md`, `TODO/pending/21_lambda_model_coupling.md`, `TODO/pending/14_full_100pocket_paper_r13.md`, `TODO/pending/19_user_decisions.md`, `TODO/pending/24_cfm_architecture_redo_plan.md`, `TODO/pending/25_round14_lit_grounded_plan.md`, `molmetal/reports/wf_pivot_a/final_phase2_bib_s7.md`.
- **§7.1 Lambda×CFM coupling (PROMOTED to position 1, WF-Pivot-A Phase 2 2026-09-16, `sec:future:lambda-cfm-coupling`)**: TODO-21 path now leads §7 because the de novo typed-term MCTS column is the paper's primary generator and Lambda×CFM coupling is the natural extension (not a competitor).
- **§7.2 CFM geometric generator (relocated from §3.6, `sec:future:cfm-deferred`)**: stub-only anchor preserving cross-references from §4/§6; full content (5 research gaps + path-(b) decoder rework + lit-grounded YuelBond/FlowMol3 path-forward) lives here, NOT in §3.
- **§7.3 Pocket-conditioned SBDD extension (NEW WF-Pivot-A Phase 2 2026-09-16, `sec:future:pb-pocket-sbdd`)**: explicit future-work item explaining that making the path pocket-mandatory requires (i) sub-fix A decode_ratio ≥ 0.5, (ii) sub-fix B+C Vina lift to -9..-11 kcal/mol, (iii) structural fix to the 3-layer singleton attractor (F2(a) MetalLigandExchange + diversity-bonus + seed-exclusion metric split). Back-points to §3 (`sec:method:secondary-cfm`), §4.11 (`sec:evaluation:hybrid-wip`), §6 (pocket-invariance limitation item).

## Reading order and bundle
- The paper is intended to be `\input`-ed into `paper/main.tex` in numeric order (§1 → §2 → §3 → §4 → §5 → §6 → §7).
- §3 (`03_method.tex`) is a master file that uses `\input{}` to compose four sub-section fragments: `03_1_mlc_formalism.tex`, `03_2_click_chemistry.tex`, `03_3_metal_geometry_prior.tex`, `03_4_mcts_search.tex`. No placeholders remain in §3.
- All four shipped sections use \textbf{MEASURED} vs \textbf{PROJECTED} labels verbatim from the upstream reports; no number is re-derived for this paper.