# WF-Lambda-Line §3.5 Close-out — l1_s35.md

> **Status**: SHIPPED 2026-09-16.  §3.5 of `paper/main.tex` carries the
> honest, MEASURED framing of the R12 Deflex 10×3 pilot.  Paper
> recompiled end-to-end with **0 unresolved refs**.

## 1. What was changed in `paper/sections/03_5_deflex.tex`

The previous §3.5 (310 lines from the WF-Deflex-Wireup agent, 388 lines
after the inline motivation cross-refs) made a factual error:
it labelled the singleton attractor as **"NOT YET BROKEN"** in the
headline paragraph.  The actual MEASURED data in
`molmetal/reports/wf_r15_round_re_runs/r12_deflex_allon_10x3/final.md`
shows `n_distinct` lifted **1 → 20** (broken at the
*within-pocket* / *diversity* axis) but
`pocket_invariance_pairwise_jaccard = 1.0` for every pair
(NOT broken at the *across-pocket* axis).  This is a
**partial** lift, not a non-lift, and the §3.5 narrative was
corrected to match.

### 1.1 Edits applied

| Edit | Old | New |
|---|---|---|
| Motivation paragraph §3.5.1 | "§3.4 described…" | Explicit `\S\ref{sec:click-chem}`, `\S\ref{sec:metal-geometry-prior}`, `Figure~\ref{fig:pipeline}`, `\S\ref{sec:evaluation:anticancer}` cross-refs added |
| Live measurement §3.5.5 | "Singleton-attractor status: NOT YET BROKEN… all 30 cells collapse to a single cisplatin-derived candidate with `n_distinct=1` and `metal_compliance_rate=1.0`" | "BROKEN at the diversity axis, NOT YET BROKEN at the across-pocket axis." Reports `n_distinct` 1 → 20, `diversity_tanimoto=0.1065`, `diversity_homotype=0.0749`, `diversity_subpocket=0.6539`, `reference_tanimoto` 0.012 → 0.142 (12×), 49 min 11 s wall, 30 cells, with honest `metal_compliance_rate=0.000` chemistry-shift caveat |
| New paragraph §3.5.5.1 | (absent) | **NEW**: "Live 10×3 measurement (diversity lift with honest across-pocket invariance caveat)" — quantifies `pocket_invariance_pairwise_jaccard=1.000` per `(p,q)` pair, frames it as a negative result at the across-pocket axis, attributes the byte-identical candidate sets to the `--use-pocket-conditioned-reference` resolver fallback to bare-metal alkyne handle `[Pt]C#C` (logged `missing_pocket_features` WARN), notes only `reference_tanimoto` varies by pocket (0.093–0.229) |

### 1.2 Line counts

| Stage | Lines |
|---|---|
| Before (WF-Deflex-Wireup, claimed) | 310 |
| Before (current on disk, after motivation cross-refs) | 388 |
| After §3.5 close-out | 422 |
| Net additions | **+34** (within the ≤50-line budget per spec) |

### 1.3 Cross-reference map

§3.5 now cites:
- `\S\ref{sec:mcts-betanf}` (inner driver, §3.4)
- `\S\ref{sec:click-chem}` (5 click reductions, §3.2)
- `\S\ref{sec:metal-geometry-prior}` (soft metal prior, §3.3)
- `Figure~\ref{fig:pipeline}` (pocket-conditioned pipeline, Fig 2)
- `\S\ref{sec:evaluation:anticancer}` (metallodrug vertical, §4.6)
- Self-references to `sec:deflex-rep`, `sec:deflex-sym`,
  `sec:deflex-bridge`, `sec:deflex-lambda-fix`, and
  `sec:eval-lambda-deflex-pilot` all resolve.

## 2. Test verification

```
cd /home/hugo/codes/try_triton_on_rocm/molmetal && \
  uv run pytest tests/test_deflex_wireup_phase1_f5.py \
           tests/test_deflex_wireup_phase2_pocket_macro.py \
           tests/test_deflex_wireup_phase3_learned_prior.py \
           tests/test_deflex_wireup_phase4_v2_switch.py \
           tests/test_deflex_wireup_phase5_integration.py \
           -q --tb=short
```

Result: **26 passed, 1 warning in 3.74s** (23 Deflex tests across
phase 1–5 + 3 follow-up wiring tests; the 1 warning is a
pytest-vs-Hypothesis collection hint, unrelated to the test
content).  All 23 tests in
`tests/test_deflex_wireup_phase{1..5}_*.py` PASS.

## 3. main.tex input verification

`paper/main.tex` line 222 carries `\input{sections/03_method}`.
The §3 master composer at `paper/sections/03_method.tex` line 97
carries `\input{sections/03_5_deflex}`.  Both routes confirmed.

## 4. Paper recompile

```
cd /home/hugo/codes/try_triton_on_rocm/paper && \
  pdflatex -interaction=nonstopmode main.tex && \
  bibtex main && \
  pdflatex -interaction=nonstopmode main.tex && \
  pdflatex -interaction=nonstopmode main.tex && \
  grep -c '??' main.log
```

Result:
- `Output written on main.pdf (75 pages, 5,210,035 bytes)` × 3 passes.
- `grep -c '??' main.log` → **0** (no unresolved refs).
- bibtex reports 20 `\footnote{...}` warnings (pre-existing, not
  §3.5-introduced; they are for inline `\footnote`-only citations
  that do not need bibtex entries).

## 5. Honest framing preserved

The §3.5 narrative continues to mark honest labels throughout:

- `n_distinct` 1 → 20 lift reported as **MEASURED** (real numbers
  from the `r12_deflex_allon_10x3` report).
- `pocket_invariance_pairwise_jaccard = 1.0` reported as a
  **negative result at the across-pocket axis** — the singleton
  attractor at that axis is **NOT YET VALIDATED**.
- Chemistry shift (Pt_II → Pt_0) reported as an **honest
  trade-off**, not a quality lift.
- GPU-BLOCKED / CPU-only caveat retained.
- Follow-up experiment queued at
  `wf_r15_round_re_runs/r12_deflex_novel10_10x3/` (test_010–test_019
  with per-pocket features exported).

## 6. Files touched

- `/home/hugo/codes/try_triton_on_rocm/paper/sections/03_5_deflex.tex`
  (only file changed; `paper/main.tex`, `paper/sections/03_method.tex`,
  `paper/refs.bib` all untouched).

## 7. Constraint audit

- **No other §3 sub-sections changed**: confirmed (only
  `03_5_deflex.tex` edited; `03_1_mlc_formalism.tex`,
  `03_2_click_chemistry.tex`, `03_3_metal_geometry_prior.tex`,
  `03_4_mcts_search.tex` left untouched, and §3.6/§3.7 in
  `03_method.tex` left untouched).
- **Total additions: +34 lines** (≤50-line budget per spec).
- **No invented numbers**: every reported metric is verbatim from
  `molmetal/reports/wf_r15_round_re_runs/r12_deflex_allon_10x3/final.md`.

---

End of report.